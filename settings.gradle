pluginManagement {
    repositories {
        maven {
            url 'https://maven.aliyun.com/repository/public'
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'jenkins-pipeline-shared-libraries'
